import React from "react";


const Features = ( { title, name } ) => {
    return (
        <features>
            <h1>{title}</h1>
            <h1>{name}</h1>
        </features>
    )
} 

export default Features;